<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kedu.project.mapper.MemberMapper">

	<select id="login" resultType="MemberDto">
	
	select memberid, pwd, name, email, admin, birth, phone, addr from member where memberid = #{memberid} and pwd = #{pwd}
	
	</select>
	
	<update id="keepLogin">
	update member set sessionkey = #{sessionId}, sessionLimit = #{next} where memberid = #{memberid}
	</update>

	<select id="checkUserWithSessionKey" resultType="MemberDto">
	select * from member where sessionkey = #{value} and sessionlimit > now()
	</select>
	
	<insert id="join">
	insert into member (memberid, pwd, name, email, admin, phone, addr)
	values (#{memberid}, #{pwd}, #{name}, #{email}, #{admin}, #{phone}, #{addr})
	
	</insert>
</mapper>